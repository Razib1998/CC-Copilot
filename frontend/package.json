{
  "name": "cc-cockpit-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host",
    "build": "vite build",
    "preview": "vite preview --port 3000 --host"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  }
}
