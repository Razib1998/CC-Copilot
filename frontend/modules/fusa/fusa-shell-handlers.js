/**
 * Platzhalter: früher Projekt-Dropdown in der Shell — entfernt (Ein-Projekt-Modell).
 * @param {ParentNode|null|undefined} _mount
 * @param {() => void|Promise<void>} [_onRefresh]
 */
export function attachFusaShellHandlers(_mount, _onRefresh) {}
